/**
 The Campaign Manager View instance is a shell that acts as StackView Fade instance for Campaign selections
 between it and resources / help / settings etc
 @class CampaignManagerView
 @constructor
 **/
define(['backbone'], function (Backbone) {
    var CampaignManagerWaspView = Backbone.View.extend({
    })

    return CampaignManagerWaspView;
});
